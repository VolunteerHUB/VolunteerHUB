extends ../layout

block content
  h1 Participating List
  br
  div(class='panel panel-default')
    table(class='table table-hover')
      th Name
      each project in data
        tr
          td
            a(href='/user/#{project.username}') #{project.displayName}
