extends ../layout

block content
  h1 Participating List
  br
  div(class='panel panel-default')
    table(class='table table-hover')
      th Name
      each project in data
        tr
          td
            a(href='/project/#{project.project_id}') #{project.project_name}
